specVersion: "v1"
serviceId: "panel"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "panel-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
